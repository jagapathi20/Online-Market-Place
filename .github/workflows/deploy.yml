name: Deploy to GCP VM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.GCP_SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.GCP_VM_HOST }} >> ~/.ssh/known_hosts

      - name: Write .env file on VM
        run: |
          echo "${{ secrets.DJANGO_ENV }}" > .env
          scp -i ~/.ssh/deploy_key .env ${{ secrets.GCP_VM_USER }}@${{ secrets.GCP_VM_HOST }}:~/Online-Market-Place/.env

      - name: Deploy
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.GCP_VM_USER }}@${{ secrets.GCP_VM_HOST }} << 'ENDSSH'
            cd ~/Online-Market-Place
            git pull origin main
            docker compose up -d --build
            docker compose exec -T web python manage.py migrate
            docker compose exec -T web python manage.py collectstatic --noinput
          ENDSSH
